<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="account_payment_email_view_form" model="ir.ui.view">
            <field name="name">account.payment.email.view.form</field>
            <field name="model">account.payment.email</field>
            <field name="arch" type="xml">
                <form string="Send Email">
                    <group col="1">
                        <group col="2">
                            <field name="partner_ids"
                                widget="many2many_tags"
                                placeholder="Add existing contacts..."
                                context="{'force_email':True, 'show_email':True, 'no_create_edit': True}"/>
                        </group>
                        <group col="2">
                            <field name="subject" placeholder="Subject..."/>
                        </group>
                        <field name="body" options="{'style-inline': true}"/>
                        <group>
                            <group>
                                <field name="attachment_ids" widget="many2many_binary"/>
                            </group>
                            <group>
                                <field name="template_id" label="Use template" context="{'default_model': 'account.payment.email'}"/>
                            </group>
                        </group>
                    </group>
                    <footer>
                        <button string="Send" name="action_send" type="object" class="btn-primary"/>
                        <button string="Cancel" class="btn-secondary" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>
    </data>
</odoo>
